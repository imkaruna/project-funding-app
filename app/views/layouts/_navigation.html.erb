<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Project name</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-links">
        <li class="active"><a href="#">Start a Project</a></li>
        <li><a href="#">Fund a Project</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">

        <% if user_signed_in? %>
        <%= link_to "Log out", destroy_user_session_path, :class => "navbar-text", :method => :delete %>
        <% else %>
        <%= link_to "Sign Up", new_user_registration_path, :class => "navbar-text"  %>
        <%= link_to "Log in", new_user_session_path(current_user), :class => "navbar-text"  %>
        <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), :class => "navbar-text"  %>
        <% end %>
      </ul>
      <ul>

      </ul>
</div><!--/.nav-collapse -->
</div>
</nav>
